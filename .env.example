# Foldspace (fallback — agents are now configured per-agent in the UI)
FOLDSPACE_URL=https://app.foldspace.ai/
FOLDSPACE_API_URL=https://app-be.foldspace.ai
FOLDSPACE_USERNAME=
FOLDSPACE_PASSWORD=

# MongoDB
MONGODB_URI=mongodb://localhost:27017/ground-truth

# Anthropic (LLM Judge)
ANTHROPIC_API_KEY=
JUDGE_MODEL=claude-sonnet-4-20250514

# Firebase (Backend — from service account JSON)
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=

# Firebase (Frontend — from Firebase console > Project settings > General)
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=

# Server
PORT=3001

# CORS — set to your frontend URL in production
# CORS_ORIGIN=https://eval.foldspace.ai

# Frontend — set to your backend URL
# NEXT_PUBLIC_API_URL=https://eval.foldspace.ai/api
