version: "3.9"

services:
  mongodb:
    image: mongo:7
    container_name: ground-truth-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    restart: unless-stopped

volumes:
  mongo-data:
